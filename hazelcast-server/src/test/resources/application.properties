spring.profiles: junit

# if no driver is specified, H2 is selected as it's in the POM
#spring.datasource.url=jdbc:h2:mem:hazelcast_store
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver

hazelcast.group hazelcast-local-junit
hazelcast.password hazelcast-local-password-junit
hazelcast.port 5701
hazelcast.multicast.enabled false
hazelcast.multicast.port 54327

