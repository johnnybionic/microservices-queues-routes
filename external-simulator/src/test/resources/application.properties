spring.profiles: junit

# if no driver is specified, H2 is selected as it's in the POM

activemq.url vm://localhost?broker.persistent=false
document.queue document.request.queue
document.ack.queue document.request.ack.queue